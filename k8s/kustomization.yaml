apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: video-creator

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - postgresql-statefulset.yaml
  - redis-deployment.yaml
  - minio-statefulset.yaml
  - api-deployment.yaml
  - worker-deployments.yaml
  - frontend-deployment.yaml
  - ingress.yaml

commonLabels:
  app: marketing-video-agent
  version: v1
