# worker_translify — Chinese-to-Vietnamese Video Pipeline
# GPU-accelerated: Whisper + PaddleOCR + IOPaint + FFmpeg NVENC/NVDEC

# ── ASR (Speech-to-Text) ──────────────────────────────────────────
faster-whisper>=1.0.0

# ── OCR (Hardcoded text detection) ───────────────────────────────
paddleocr>=2.7.3
paddlepaddle

# ── TTS (Text-to-Speech) — network API, 0 VRAM ───────────────────
edge-tts>=6.1.9

# ── Audio Separation (vocals / BGM) ──────────────────────────────
audio-separator[gpu]>=0.17.0

# ── Image / Video processing ──────────────────────────────────────
Pillow
ffmpeg-python>=0.2.0
numpy>=1.24.0
opencv-python>=4.8.0

# ── Audio time-stretching ─────────────────────────────────────────
pyrubberband>=0.3.0
soundfile>=0.12.0

# ── Ollama client (already in global env but listed for venv) ─────
ollama>=0.6.0
httpx>=0.27.0

# ── Shared infra (installed editable from root) ───────────────────
# celery redis sqlalchemy minio psycopg2-binary  (inherited from shared_core)
celery>=5.3.0
redis>=5.0.0
sqlalchemy>=2.0.0
minio>=7.2.0
psycopg2-binary>=2.9.0
